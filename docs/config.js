const modusReactBootstrapVersion = "1.0.0"
// require("@trimbleinc/modus-react-bootstrap/package.json").version

const config = {
  modusReactBootstrapVersion,
  version: require("./package.json").version,
}

export default config
